language: python
python:
          - "3.6"

before_install:
          - sudo apt-get install -y libspatialindex-dev

cache: pip
install:
          - pip install -r requirements.txt
script:
          - python travis_tests.py

